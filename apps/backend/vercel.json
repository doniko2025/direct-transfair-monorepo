{
  "version": 2,

  "buildCommand": "pnpm vercel-build",

  "installCommand": "pnpm install --no-frozen-lockfile",

  "functions": {
    "dist/main.js": {
      "maxDuration": 30
    }
  },

  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/main.js"
    }
  ]
}