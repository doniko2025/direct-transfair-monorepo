[variables]
NODE_VERSION = "20"
NIXPACKS_METADATA = "pnpm"

[phases.setup]
nixPkgs = ["nodejs_20", "pnpm-9_x"]

[phases.install]
cmds = [
  "pnpm install --no-frozen-lockfile"
]

[phases.build]
cmds = [
  "pnpm build:backend"
]

[start]
cmd = "pnpm start:backend:prod"